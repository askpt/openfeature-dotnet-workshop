# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: workshop-solution
services:  
  app:
    language: dotnet
    project: ./src/Garage.AppHost/Garage.AppHost.csproj
    host: containerapp
